# 邀请函门票系统规划

## 系统概述

邀请函门票是博览会为VIP嘉宾、媒体、参展商等特殊群体提供的免费入场凭证，需要填写完整个人信息（姓名、身份证、电话）后生成带有二维码的电子票券，通过扫码核销入场。

---

## 一、用户申请流程

### 1.1 申请入口

**前端页面**：`invitation.html`（需新建）

**申请表单字段**：
```javascript
{
    // 必填信息
    name: String,              // 真实姓名
    idCard: String,            // 身份证号
    phone: String,             // 手机号（验证码验证）

    // 可选信息
    email: String,             // 邮箱
    company: String,           // 公司/机构
    position: String,          // 职位
    reason: String,            // 申请理由

    // 邀请信息（如有邀请码）
    inviteCode: String,        // 邀请码（可选，优先审核）
    category: String           // 申请类别（VIP/媒体/参展商/其他）
}
```

**前端验证规则**：
- 姓名：2-20个字符
- 身份证：18位，格式验证
- 手机：11位，短信验证码
- 邀请码：8位字母数字（如提供）

### 1.2 申请流程图

```
用户填写表单
    ↓
发送短信验证码
    ↓
验证短信验证码
    ↓
提交申请（含身份证信息加密）
    ↓
后端审核逻辑
    ├─ 有邀请码 → 自动通过
    ├─ 特定类别 → 自动通过
    └─ 普通申请 → 待人工审核
    ↓
发放电子票券
```

---

## 二、后端API设计

### 2.1 邀请函申请接口

**请求**：
```http
POST /api/v1/invitations/apply
Content-Type: application/json

{
    "applicantInfo": {
        "name": "张三",
        "idCard": "220102199001011234",  // 加密传输
        "phone": "13800138000",
        "smsCode": "123456",
        "email": "zhangsan@example.com",
        "company": "某科技公司",
        "position": "产品经理",
        "reason": "媒体采访",
        "inviteCode": "VIP202401"  // 可选
    }
}
```

**响应**：
```json
{
    "code": "SUCCESS",
    "message": "申请提交成功",
    "data": {
        "applicationId": 123,
        "applicationNo": "INV20260424001",
        "auditStatus": "APPROVED",  // APPROVED/PENDING/REJECTED
        "ticket": {  // 自动通过时返回
            "ticketNo": "TK20260424ABC123",
            "qrCodeToken": "eyJ0aWNrZXRObyI6..."
        }
    }
}
```

### 2.2 审核状态查询

```http
GET /api/v1/invitations/status/:applicationNo
```

### 2.3 我的邀请函列表

```http
GET /api/v1/invitations/my-tickets?phone=13800138000
```

---

## 三、二维码生成

### 3.1 二维码内容设计

**方案A：加密Token（推荐）**
```json
{
    "type": "invitation",
    "ticketNo": "TK20260424ABC123",
    "userId": "U123456",
    "timestamp": 1713945600000,
    "signature": "abc123..."  // 防篡改签名
}
```

Base64编码后作为二维码内容。

**方案B：票号（简单方案）**
```
INV-TK20260424ABC123
```

### 3.2 二维码样式设计

```
┌─────────────────────┐
│   东北亚数字文创     │
│      博览会          │
│                     │
│   ┌───────────┐     │
│   │           │     │
│   │   二维码   │     │
│   │           │     │
│   └───────────┘     │
│                     │
│  邀请函票            │
│  票号:TK...ABC123   │
│  姓名:张三          │
│  有效期:2024.06.01  │
└─────────────────────┘
```

### 3.3 二维码生成代码（后端）

```javascript
/**
 * 生成邀请函二维码
 */
async function generateInvitationQRCode(ticket) {
    const qrData = {
        type: 'invitation',
        ticketNo: ticket.ticket_no,
        userId: ticket.user_id,
        timestamp: Date.now()
    };

    // 生成token
    const token = Buffer.from(JSON.stringify(qrData)).toString('base64');

    // 生成二维码图片
    const qrCodeDataURL = await QRCode.toDataURL(token, {
        width: 300,
        margin: 2,
        color: {
            dark: '#000000',
            light: '#ffffff'
        }
    });

    // 保存到票券记录
    await pool.query(
        'UPDATE tickets SET qr_code_token = ?, qr_code_image = ? WHERE id = ?',
        [token, qrCodeDataURL, ticket.id]
    );

    return { token, qrCodeDataURL };
}
```

---

## 四、核销流程

### 4.1 核销终端界面

**位置**：`public/verify-terminal.html`（已存在）

**核销方式**：
1. **扫码核销**（主要）
   - 使用摄像头扫描二维码
   - 自动解析票券信息
   - 显示持票人信息供核对
   - 一键核销

2. **手机号补验**（备用）
   - 输入手机号查询
   - 显示该手机号的所有有效票券
   - 选择票券核销
   - 核对姓名后确认

### 4.2 核销API

**预校验接口**（显示票券信息）：
```http
POST /api/v1/verify/preview
Content-Type: application/json

{
    "qrContent": "eyJ0aWNrZXRObyI6...",
    "verifierInfo": {
        "verifierId": "STAFF001",
        "verifierName": "核销员A"
    }
}
```

**响应**：
```json
{
    "code": "SUCCESS",
    "data": {
        "ticketNo": "TK20260424ABC123",
        "ticketName": "邀请函票",
        "holderName": "张三",
        "holderIdCard": "220102********1234",  // 脱敏
        "holderPhone": "138****8000",
        "validStartTime": "2024-06-01 09:00:00",
        "validEndTime": "2024-06-03 18:00:00",
        "ticketStatus": "ACTIVE",
        "category": "INVITATION"
    }
}
```

**核销确认接口**：
```http
POST /api/v1/verify/confirm
Content-Type: application/json

{
    "ticketNo": "TK20260424ABC123",
    "verifierInfo": {
        "verifierId": "STAFF001",
        "verifierName": "核销员A",
        "deviceId": "TERMINAL01",
        "location": "A入口"
    }
}
```

### 4.3 核销记录

核销成功后保存以下信息：
```javascript
{
    ticketId: 123,
    ticketNo: "TK20260424ABC123",
    verifyType: "QR_CODE",  // QR_CODE/MANUAL
    verifyStatus: "SUCCESS",
    verifyTime: "2024-06-01 10:30:00",
    verifyLocation: "A入口",
    verifierId: "STAFF001",
    verifierName: "核销员A",
    deviceId: "TERMINAL01"
}
```

---

## 五、数据库设计

### 5.1 邀请函申请表

```sql
CREATE TABLE invitation_applications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    application_no TEXT UNIQUE NOT NULL,        -- 申请单号
    applicant_name TEXT NOT NULL,                -- 申请人姓名
    applicant_idcard TEXT NOT NULL,              -- 身份证号（加密）
    applicant_phone TEXT NOT NULL,               -- 手机号
    applicant_email TEXT,                        -- 邮箱
    applicant_company TEXT,                      -- 公司
    applicant_position TEXT,                     -- 职位
    apply_reason TEXT,                           -- 申请理由
    invite_code TEXT,                            -- 邀请码
    category TEXT,                               -- 类别
    audit_status TEXT DEFAULT 'PENDING',         -- 审核状态
    audit_by TEXT,                               -- 审核人
    audit_time TEXT,                             -- 审核时间
    audit_remark TEXT,                           -- 审核备注
    source_type TEXT DEFAULT 'WEB',              -- 来源
    client_ip TEXT,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT DEFAULT CURRENT_TIMESTAMP
);
```

### 5.2 票券表（新增字段）

```sql
ALTER TABLE tickets ADD COLUMN id_card TEXT;              -- 身份证号（加密）
ALTER TABLE tickets ADD COLUMN id_card_hash TEXT;         -- 身份证hash（用于验证）
ALTER TABLE tickets ADD COLUMN invite_code TEXT;          -- 邀请码
ALTER TABLE tickets ADD COLUMN application_id INTEGER;     -- 关联申请ID
ALTER TABLE tickets ADD COLUMN qr_code_image TEXT;         -- 二维码图片数据
```

---

## 六、安全考虑

### 6.1 身份证信息处理

```javascript
/**
 * 身份证号加密存储
 */
const crypto = require('crypto');

const ID_CARD_SECRET = process.env.ID_CARD_SECRET || 'change-in-production';

function encryptIdCard(idCard) {
    const cipher = crypto.createCipher('aes-256-cbc', ID_CARD_SECRET);
    let encrypted = cipher.update(idCard, 'utf8', 'hex');
    encrypted += cipher.final('hex');
    return encrypted;
}

function decryptIdCard(encrypted) {
    const decipher = crypto.createDecipher('aes-256-cbc', ID_CARD_SECRET);
    let decrypted = decipher.update(encrypted, 'hex', 'utf8');
    decrypted += decipher.final('utf8');
    return decrypted;
}

function hashIdCard(idCard) {
    return crypto.createHash('sha256').update(idCard).digest('hex');
}

function maskIdCard(idCard) {
    return idCard.substring(0, 6) + '********' + idCard.substring(14);
}
```

### 6.2 二维码防伪

1. **时效性**：二维码token包含时间戳，可设置有效期
2. **一次性**：核销后二维码失效
3. **签名验证**：token包含签名，防止伪造

### 6.3 申请风控

```javascript
// 防重复申请
const hasPending = await checkPendingApplication(phone);

// 频率限制
const recentCount = await countRecentApplications(phone, 1); // 1小时内

// 身份证唯一性
const existsTicket = await checkExistingTicketByIdCard(idCard);
```

---

## 七、前端页面设计

### 7.1 申请页面（invitation.html）

**页面结构**：
```
┌─────────────────────────────────────┐
│       邀请函申请                     │
├─────────────────────────────────────┤
│                                     │
│  基本信息（必填）                   │
│  ┌─────────────────────────────┐   │
│  │ 姓名  [______________]       │   │
│  │ 身份证  [______________]     │   │
│  │ 手机  [__________] [获取验证码]│
│  │ 验证码 [______]              │   │
│  └─────────────────────────────┘   │
│                                     │
│  扩展信息（选填）                   │
│  ┌─────────────────────────────┐   │
│  │ 邮箱  [______________]       │   │
│  │ 公司  [______________]       │   │
│  │ 职位  [______________]       │   │
│  └─────────────────────────────┘   │
│                                     │
│  邀请信息（选填）                   │
│  ┌─────────────────────────────┐   │
│  │ 邀请码  [______________]     │   │
│  │ 申请类别 [VIP ▼]             │   │
│  │ 申请理由 [_______________]   │   │
│  │          [_______________]   │   │
│  └─────────────────────────────┘   │
│                                     │
│         [提交申请]                  │
└─────────────────────────────────────┘
```

### 7.2 我的邀请函页面（my-invitations.html）

显示已获得的邀请函票券列表，可查看二维码详情。

---

## 八、实施步骤

### 阶段一：后端开发（1-2天）
- [ ] 创建申请接口
- [ ] 实现审核逻辑
- [ ] 二维码生成功能
- [ ] 核销接口完善

### 阶段二：前端开发（1-2天）
- [ ] 创建申请页面
- [ ] 短信验证码集成
- [ ] 邀请函列表页面
- [ ] 二维码展示页面

### 阶段三：测试上线（1天）
- [ ] 功能测试
- [ ] 核销流程测试
- [ ] 安全测试
- [ ] 上线部署

---

## 九、配置项

```env
# 身份证加密密钥
ID_CARD_SECRET=your-secret-key-change-in-production

# 邀请码前缀（可选）
INVITE_CODE_PREFIX=VIP

# 自动审核规则
AUTO_AUDIT_WITH_INVITE_CODE=true
AUTO_AUDIT_CATEGORIES=VIP,EXHIBITOR,MEDIA
```

---

## 十、API端点汇总

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/invitations/apply` | POST | 提交邀请函申请 |
| `/api/v1/invitations/status/:no` | GET | 查询申请状态 |
| `/api/v1/invitations/my-tickets` | GET | 我的邀请函 |
| `/api/v1/sms/send` | POST | 发送验证码 |
| `/api/v1/verify/preview` | POST | 核销预校验 |
| `/api/v1/verify/confirm` | POST | 确认核销 |
| `/api/v1/verify/records` | GET | 核销记录 |

---

**文档版本**: v1.0
**创建日期**: 2024-04-24
**状态**: 待实施
