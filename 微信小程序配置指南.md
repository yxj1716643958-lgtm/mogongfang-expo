# 微信小程序配置指南

## 获取AppID和AppSecret

### 1. 获取AppID
1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入「开发」→「开发管理」→「开发设置」
3. 在「开发者ID」栏目中可以看到 `AppID(小程序ID)`

### 2. 获取AppSecret
1. 在同一页面，找到 `AppSecret(小程序密钥)`
2. 点击「重置」或「生成」（如果之前已设置过）
3. 验证管理员身份后获取
4. **重要**：AppSecret只在生成时显示一次，请立即保存

### 3. 配置服务器域名
在「开发设置」中配置以下域名白名单：

**request合法域名**：
```
https://你的后端域名.com
```

**socket合法域名**：
```
wss://你的后端域名.com
```

**uploadFile合法域名**：
```
https://你的后端域名.com
```

**downloadFile合法域名**：
```
https://你的后端域名.com
```

## 微信支付商户配置

### 4. 获取微信支付商户号
1. 登录 [微信支付商户平台](https://pay.weixin.qq.com/)
2. 在「账户中心」→「商户信息」中查看 `微信支付商户号`

### 5. 设置API密钥
1. 进入「账户中心」→「API安全」→「API密钥」
2. 设置API密钥（32位字符）
3. 保存到 `.env` 的 `WECHAT_PAY_API_KEY`

### 6. 获取API证书（可选）
1. 在「账户中心」→「API安全」→「API证书」
2. 申请并下载证书
3. 查看证书序列号，保存到 `WECHAT_PAY_CERT_SERIAL_NO`

### 7. 设置APIv3密钥
1. 在「账户中心」→「API安全」→「设置APIv3密钥」
2. 设置32位密钥，保存到 `WECHAT_PAY_API_V3_KEY`

## 配置到项目

获取以上信息后，更新 `.env` 文件：

```env
# 小程序配置
WECHAT_MINIAPP_APPID=你的AppID
WECHAT_MINIAPP_SECRET=你的AppSecret

# 微信支付配置（支付开通后配置）
WECHAT_PAY_MCH_ID=你的商户号
WECHAT_PAY_API_KEY=你的API密钥
WECHAT_PAY_CERT_SERIAL_NO=证书序列号
WECHAT_PAY_API_V3_KEY=APIv3密钥

# 启用小程序支付
WECHAT_MINIAPP_ENABLED=true
```

## 注意事项

1. **AppSecret保密**：不要提交到代码仓库，只在服务器配置
2. **域名必须HTTPS**：微信要求所有合法域名必须使用HTTPS
3. **支付申请**：微信支付需要企业资质申请，审核时间1-3天
4. **测试模式**：在支付申请期间，可以继续开发其他功能
