# 汇付支付"签名失败"问题排查报告

## 问题分析结果

### 1. 密钥验证 ✓ 通过
- 商户私钥格式正确（PKCS#8）
- 私钥与商户公钥配对正确
- 签名算法（SHA256withRSA）实现正确

### 2. API网关问题 ⚠️
当前配置的网关地址 `https://spin.cloudpnr.com` 返回404错误。

**可能原因：**
- 网关地址已变更
- API路径不正确
- 账户未开通H5支付权限

**建议操作：**
1. 登录汇付商户平台：https://mer.chinapay.com
2. 确认当前有效的API网关地址
3. 查看产品管理中H5支付是否已开通
4. 获取最新的API接口文档

### 3. 测试环境配置
沙箱环境地址：`https://hfgj.testpnr.com` 也返回404

**建议：**
- 先在沙箱环境测试
- 确认测试通过后再切换到生产环境

### 4. 当前配置状态
```bash
PAYMENT_DEBUG_MODE=false  # 已关闭调试模式
HUIFU_ENVIRONMENT=PRODUCTION  # 当前使用生产环境
```

## 解决方案

### 方案A: 使用调试模式（推荐用于开发）
如果只是想测试票务流程，可以使用调试模式：

```bash
# 在.env文件中设置
PAYMENT_DEBUG_MODE=true
```

调试模式下：
- 跳过真实汇付支付调用
- 直接模拟支付成功
- 自动生成票券

### 方案B: 修复真实支付
如需测试真实汇付支付：

1. **确认正确的网关地址**
   - 联系汇付技术支持
   - 或在商户平台查看最新的API文档

2. **更新网关配置**
   ```bash
   # 生产环境（需确认）
   HUIFU_GATEWAY_URL=https://正确的网关地址

   # 或沙箱环境
   HUIFU_GATEWAY_URL=https://hfgj.testpnr.com
   HUIFU_ENVIRONMENT=SANDBOX
   ```

3. **确认API路径**
   当前使用的路径：`/gateway/api/rest/api/transaction/h5pay`
   可能需要根据最新文档调整

## 测试命令

### 1. 测试签名生成
```bash
node test-signing.js
```

### 2. 测试API连通性
```bash
node test-huifu-urls.js
node test-huifu-sandbox.js
```

### 3. 测试完整支付流程
```bash
node ticket-server.js
# 然后在前端发起支付请求
```

## 需要确认的信息

请向汇付确认以下信息：
1. 当前有效的API网关地址是什么？
2. H5支付的完整API路径是什么？
3. 测试环境（沙箱）的网关地址和接入方式？
4. 账户是否已开通H5支付产品？

## 临时建议

在等待确认网关地址期间，建议：
1. 使用 `PAYMENT_DEBUG_MODE=true` 测试其他功能
2. 确保订单创建、票券生成等逻辑正常
3. 网关地址确认后再进行真实支付测试
