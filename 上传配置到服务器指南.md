# 上传配置到服务器指南

## 方法一：使用 PuTTY（推荐）

1. 下载 PuTTY：https://www.putty.org/
2. 打开命令提示符，执行：
   ```
   pscp -pw zh12345678Q E:\魔攻方\ 第二次尝试\.env root@101.200.126.62:/opt/mogongfang/.env
   ```
3. 上传完成后，重启服务：
   ```
   putty -ssh root@101.200.126.62 -P 22 -pw zh12345678Q "pm2 restart ticket-service"
   ```

## 方法二：手动操作

1. 使用SSH工具连接服务器：
   - 服务器：101.200.126.62
   - 用户：root
   - 密码：zh12345678Q

2. 连接后执行：
   ```bash
   cd /opt/mogongfang
   cat > .env << 'EOF'
   （粘贴本地.env文件内容）
   EOF
   pm2 restart ticket-service
   pm2 logs ticket-service --lines 20
   ```

## 方法三：双击运行批处理文件

双击运行 `deploy-env-to-server.bat`（需要先安装PuTTY）

## 上传后验证

访问：https://101.200.126.62/health

应该看到服务正常运行。
