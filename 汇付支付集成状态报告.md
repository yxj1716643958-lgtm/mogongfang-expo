# 汇付支付集成状态报告

**报告日期**: 2026-04-24
**测试状态**: ⚠️ 部分完成 - 需要进一步确认

## 执行摘要

已成功将汇付支付集成从旧版Spin平台迁移到新版斗拱平台API，但API端点返回405错误，表明请求格式需要进一步确认。

## 已完成的工作

### 1. ✅ 代码更新完成

**更新的文件**: `ticket-server.js`

**主要更改**:
- 网关地址更新: `https://spin.cloudpnr.com` → `https://paas.huifu.com`
- API端点更新: `/gateway/api/rest/api/transaction/acqTrade` → `/v2/trade/hosting/payment/preorder`
- 请求格式更新: 使用斗拱平台托管收银台V2 API格式
- 响应处理增强: 支持V1和V2多种响应格式
- 回调处理增强: 支持多种字段名和成功码

### 2. ✅ 网关验证完成

**测试结果**:
```
✓ https://paas.huifu.com/ - 200 OK
✓ https://paas.huifu.com/health - 200 OK
⚠️ https://paas.huifu.com/v2/trade/hosting/payment/preorder - 405 Method Not Allowed
```

**结论**: 斗拱平台域名正确，API端点存在，但请求格式需要调整。

## 当前配置

```env
# 汇付天下配置
HUIFU_MERCHANT_ID=6666000195047270
HUIFU_SYS_ID=6666000195047270
HUIFU_APP_ID=KAZX
HUIFU_GATEWAY_URL=https://paas.huifu.com
HUIFU_ENVIRONMENT=PRODUCTION

# 支付调试模式
PAYMENT_DEBUG_MODE=false  # 已关闭，使用真实支付
```

## 问题分析

### 405错误原因

**症状**: API端点返回 `405 Method Not Allowed`

**可能原因**:
1. 请求格式不正确（body结构、字段名称）
2. 缺少必需的HTTP头（如认证token）
3. API路径需要特殊的前缀或版本号
4. 商户未开通"托管收银台"产品权限

**已排除**:
- ❌ 网关地址错误（已验证paas.huifu.com正确）
- ❌ 端点不存在（404错误已被排除）
- ❌ 网络连接问题（能访问根路径）

## 解决方案选项

### 选项A: 获取官方API文档（推荐）

1. **登录商户平台**
   - 访问: https://mer.chinapay.com
   - 查找API文档或开发中心

2. **联系技术支持**
   - 确认"托管收银台"产品开通状态
   - 获取最新的API接入文档
   - 确认正确的请求格式和路径

3. **参考官方示例**
   - GitHub: huifurepo/dg-payment-skills
   - 查找Node.js/JavaScript示例代码

### 选项B: 使用调试模式（临时方案）

在等待API确认期间，可以使用调试模式测试其他功能：

```env
PAYMENT_DEBUG_MODE=true
```

调试模式下会跳过真实支付，直接生成票券。

### 选项C: 尝试其他支付方式

如果托管收银台产品未开通，可以尝试：
- 聚合正扫支付（acqTrade接口）
- H5支付接口
- 微信/支付宝小程序支付

## 代码变更摘要

### ticket-server.js 主要更新

```javascript
// 1. 网关配置
gatewayUrl: 'https://paas.huifu.com'  // 新斗拱平台

// 2. 请求数据格式
{
    req_seq_id: string,      // 请求流水号
    req_date: string,        // 请求日期
    huifu_id: string,        // 商户号
    pre_order_type: '1',     // 1=H5支付, 2=支付宝小程序, 3=微信小程序
    trans_amt: string,       // 金额（分）
    goods_desc: string,      // 商品描述
    mer_ord_id: string,      // 商户订单号
    time_expire: string,     // 过期时间
    notify_url: string,      // 回调地址
    risk_check_data: string  // 风控数据（JSON字符串）
}

// 3. API端点
POST /v2/trade/hosting/payment/preorder
```

## 测试脚本

已创建以下测试脚本用于验证：

1. **test-douguo-api.js** - 测试斗拱平台API
   ```bash
   node test-douguo-api.js
   ```

2. **test-huifu-url-variants.js** - 测试多种可能的URL
   ```bash
   node test-huifu-url-variants.js
   ```

3. **search-api-docs.js** - 搜索API文档
   ```bash
   node search-api-docs.js
   ```

## 下一步行动

### 立即行动

1. **联系汇付技术支持**
   - 确认商户号 6666000195047270 的产品开通情况
   - 确认"托管收银台"API的正确接入方式
   - 获取测试环境和生产环境的接入文档

2. **检查商户平台**
   - 登录 https://mer.chinapay.com
   - 查看产品管理中是否有"托管收银台"
   - 查看API文档或SDK下载

### 短期计划

1. 获取正确的API文档后，更新请求格式
2. 在测试环境验证API调用
3. 完成真实支付流程测试

### 长期计划

1. 完善错误处理和日志记录
2. 实现订单状态同步机制
3. 添加支付失败重试逻辑

## 技术支持联系人

- 汇付商户平台: https://mer.chinapay.com
- 官方GitHub: https://github.com/huifurepo/dg-payment-skills
- 技术支持: （需要登录商户平台获取）

## 附录：健康检查响应

```json
{
  "code": "SUCCESS",
  "message": "服务运行正常",
  "data": {
    "status": "ok",
    "timestamp": "2026-04-24T03:39:42.774Z",
    "database": "SQLite",
    "huifu": "configured",
    "huifuGateway": "https://paas.huifu.com",
    "huifuEnvironment": "PRODUCTION",
    "paymentDebugMode": false
  }
}
```

---

**结论**: 代码已更新完成，API端点已找到，但需要获取官方文档确认正确的请求格式。建议优先联系汇付技术支持获取最新API接入文档。
